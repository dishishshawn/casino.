[project]
name = "casino"
version = "0.1.0"
description = "Claude-powered retail trading system: Earnings-Drift LLM Long-Short."
requires-python = ">=3.11"
authors = [{ name = "Casino" }]
readme = "README.md"
dependencies = [
    "alpaca-py>=0.43.4",
    "anthropic>=0.97.0",
    "backtrader>=1.9.78.123",
    "beautifulsoup4>=4.14.3",
    "discord-webhook>=1.4.1",
    "duckdb>=1.5.2",
    "httpx>=0.28.1",
    "loguru>=0.7.3",
    "lxml>=6.1.0",
    "pyarrow>=24.0.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.14.0",
    "python-dotenv>=1.2.2",
    "streamlit>=1.57.0",
    "vectorbt>=1.0.0",
    "yfinance>=1.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["casino"]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["notebooks", ".venv", "build"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]  # line length handled by formatter

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
warn_unreachable = true
disallow_untyped_defs = true
no_implicit_optional = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["vectorbt.*", "duckdb.*", "backtrader.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# backtrader's Strategy uses `params` as a tuple-of-tuples descriptor that
# mypy cannot see through; we relax subclassing rules on bt_validate only.
module = ["casino.backtest.bt_validate"]
disallow_subclassing_any = false
warn_unused_ignores = false

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
addopts = "-ra"
markers = [
    "release_gate: tests guarding LLM look-ahead bias (PRD §6.3) — must always pass",
]

[dependency-groups]
dev = [
    "ruff>=0.6",
    "mypy>=1.10",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ipykernel>=6.29",
    "responses>=0.25",
    "pytest-asyncio>=0.23",
    "types-beautifulsoup4>=4.12.0.20250516",
    "nbconvert>=7.17.1",
    "pandas-stubs>=3.0.0.260204",
]
